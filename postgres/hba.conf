# Allow replication connections from standby servers
#host    replication     replicator      10.0.1.101/32           md5
#host    replication     replicator      10.0.1.102/32           md5

# Allow replication from Docker containers
# host    replication     replicator      172.16.0.0/12           md5
# host    replication     replicator      192.168.0.0/16          md5
# Allow specific users to specific databases
host    all   all 0.0.0.0/0   md5
host    all   all 0.0.0.0/0   md5
host    all all 0.0.0.0/0   scram-sha-256